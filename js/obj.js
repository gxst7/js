let userObj = {
    name: "alexa",
    age: "undefined",
    sex: "undefined",
    "net worth": 5000,
};

let keySex = "sex";

alert(userObj.name);
alert(userObj["age"]);
alert(userObj[keySex]);
alert(userObj["net worth"]);